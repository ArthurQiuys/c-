day 2 20160226
1.c++内存布局
    栈区，堆区，全局/静态区，文字常量区（RO)，代码区(RO)(对这两个的操作是属于非法的操作）
2.类和对象
    -类的定义
    -private/public/protected
    -类的实现-->成员函数的实现（就是是有实体进行继承)-->类里面或者类外面的定义
    -构造函数：默认构造函数、有参构造函数、复制构造函数、
    -析构函数：（特殊函数）
    -赋值运算符函数
    【后3者如果没有自己写函数，系统会提供默认的函数
